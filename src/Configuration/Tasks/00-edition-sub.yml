---
title: EditionSub
description: Must run FIRST â€” eb-tool.exe validates EditionSubVersion/Manufacturer
privilege: TrustedInstaller
actions:

  - !writeStatus: {status: 'Setting EditionSub values for eb-tool compatibility'}
  - !powerShell:
    wait: true
    command: "New-ItemProperty -Path 'HKLM:\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion' -Name 'EditionSubManufacturer' -Value 'EBTool' -PropertyType String -Force | Out-Null; New-ItemProperty -Path 'HKLM:\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion' -Name 'EditionSubVersion' -Value 'EBOS' -PropertyType String -Force | Out-Null"
